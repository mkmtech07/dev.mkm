@extends('layouts.admin')
@section('title', 'Schema Markup')
@section('page-title', 'Schema Markup')
@section('content')
<div class="d-flex flex-column flex-sm-row justify-content-between align-items-sm-center gap-3 mb-4"><div><h2 class="h5 mb-1">JSON-LD schema markup</h2><p class="text-secondary mb-0">Manage structured data published through the frontend.</p></div><a class="btn btn-primary" href="{{ route('admin.website.seo.schema.create') }}">Add schema</a></div>
<div class="card content-card"><div class="card-header"><form class="row g-2" method="GET"><div class="col-sm-6 col-lg-5"><input class="form-control" name="search" type="search" value="{{ $search }}" placeholder="Search name or type" aria-label="Search schemas"></div><div class="col-sm-4 col-lg-3"><select class="form-select" name="type"><option value="">All types</option>@foreach (\App\Models\SchemaMarkup::TYPES as $item)<option value="{{ $item }}" @selected($type === $item)>{{ $item }}</option>@endforeach</select></div><div class="col-auto"><button class="btn btn-outline-primary">Filter</button></div>@if($search !== '' || $type !== '')<div class="col-auto"><a class="btn btn-light" href="{{ route('admin.website.seo.schema.index') }}">Clear</a></div>@endif</form></div><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Name</th><th>Type</th><th>Order</th><th>Status</th><th>Updated</th><th class="text-end">Actions</th></tr></thead><tbody>@forelse($schemaMarkups as $schemaMarkup)<tr><td class="fw-semibold">{{ $schemaMarkup->name }}</td><td><span class="badge text-bg-primary">{{ $schemaMarkup->type }}</span></td><td>{{ $schemaMarkup->sort_order }}</td><td><form method="POST" action="{{ route('admin.website.seo.schema.toggle-status', $schemaMarkup) }}">@csrf @method('PATCH')<button class="btn btn-sm {{ $schemaMarkup->status ? 'btn-success' : 'btn-outline-secondary' }}">{{ $schemaMarkup->status ? 'Active' : 'Inactive' }}</button></form></td><td>{{ $schemaMarkup->updated_at->format('d M Y') }}</td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.website.seo.schema.edit', $schemaMarkup) }}">Edit</a> <form class="d-inline" method="POST" action="{{ route('admin.website.seo.schema.destroy', $schemaMarkup) }}" onsubmit="return confirm('Delete this schema markup?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></td></tr>@empty<tr><td colspan="6" class="py-5 text-center text-secondary">{{ $search !== '' || $type !== '' ? 'No schema markups match your filters.' : 'No schema markups have been created yet.' }}</td></tr>@endforelse</tbody></table></div>@if($schemaMarkups->hasPages())<div class="card-footer py-3">{{ $schemaMarkups->onEachSide(1)->links('pagination::bootstrap-5') }}</div>@endif</div>
@endsection
